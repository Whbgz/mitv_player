plugins {
    id "com.android.application"
}

android {
    namespace "mitv.player"
    compileSdk 35

    defaultConfig {
        applicationId "mitv.player"
        minSdk 23
        targetSdk 35
        versionCode 1
        versionName "1.0.0"

        ndk {
            abiFilters "armeabi-v7a"
        }
    }
}
